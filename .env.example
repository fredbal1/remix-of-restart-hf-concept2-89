# No runtime environment variables are currently required.
# Formspree is intentionally configured via src/config/formspree.ts.
# The endpoint is a public frontend config value embedded at build time.
# Exporting dist/ to OVH does not require an additional env step for the contact form.
